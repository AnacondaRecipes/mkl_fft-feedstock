# upload to a temp channel for testing
channels:
  cbouss: mkl2023 

# the conda-build parameters to use for disabling --skip-existing
build_parameters:
  - "--suppress-variables"
  #- "--skip-existing"
  - "--error-overlinking"
  #- "--error-overdepending"
